<?xml version="1.0" encoding="UTF-8"?>
<CarouselPage 
		xmlns="http://xamarin.com/schemas/2014/forms" 
		xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
		x:Class="CarouselPageNavigation.MainPage">
	
	<CarouselPage.ItemTemplate>
		<DataTemplate>
			<ContentPage>
				<ContentPage.Padding>
					<OnPlatform x:TypeArguments="Thickness" iOS="0,40,0,0" Android="10,0,10,0" />
				</ContentPage.Padding>
				<StackLayout>
					<Label x:Name="lblName" Text="{Binding Name}" FontSize="Medium" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="Center" HeightRequest="30"  BackgroundColor="Black" TextColor="White"  />
					<!--<BoxView  Color="{Binding Color}" WidthRequest="200" HeightRequest="200" HorizontalOptions="Center" VerticalOptions="Center" />-->
					<Button 
						BackgroundColor="{Binding Color}" 
						BorderRadius="20" 
						Text="Acquista" 
						WidthRequest="200" 
						HeightRequest="200" 
						HorizontalOptions="Center" 
						Clicked="OnBuyClicked"
						VerticalOptions="CenterAndExpand"/>
					<Label Text="{Binding Description}" FontSize="Medium" HorizontalOptions="StartAndExpand" VerticalOptions="Start"  />
					<Label Text="{Binding Price}" FontSize="Medium" HorizontalOptions="StartAndExpand" VerticalOptions="StartAndExpand"  />
				</StackLayout>
			</ContentPage>
		</DataTemplate>
	</CarouselPage.ItemTemplate>
</CarouselPage>

